// Implement a graph generator that represents a set of objects where some pairs of objects are connected by links.

// tests whether there is an edge from the vertices x to y;
function adjacent(G, x, y) {

}

// lists all vertices such that there is an edge from the vertices x to y
function neighbors(G, x) {

}


// adds the vertex x, if it is not there;
function add_vertex(G, x) {

}

// removes the vertex x, if it is there;
function remove_vertex(G, x){}
